1.1 - Determine if a string has all unique characters. What if you cannot use additional data structures?

1.2 - Given two strings, decide if one is a permutation of the other.

1.3 - Replace all spaces in a string with '%20'.

1.4 - Check if a string is a permutation of a palindrome.

1.5 - One Away: You can Edit, Remove or Insert in a String. Check if the string is one edit (or zero) away. 

1.6 - String Compression: Compress the string with the format a3b1c2 (aaabcc).

1.7 - Rotate a NxN matrix by 90 degrees.

1.8 - Write an algorithm such that if an element in an MxN matrix is 0, its entire row and column are set to 0.

1.9 - String rotation: Check if a string s2 is a rotation of a string s1. (waterbottle and erbottlewat).